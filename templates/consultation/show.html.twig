{% extends 'base.html.twig' %}

{% block title %}Consultation{% endblock %}

{% block body %}
    <h1>Consultation</h1>
<br><br><br><br><br>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consultation.id }}</td>
            </tr>
            <tr>
                <th>Date_consultation</th>
                <td>{{ consultation.dateConsultation }}</td>
            </tr>
            <tr>
                <th>Dossiermedical_id</th>
                <td>{{ consultation.dossiermedical }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ consultation.Email }}</td>
            </tr>
         
         
        </tbody>
    </table>

    <a href="{{ path('app_consultation_index') }}">back to list</a>

    <a href="{{ path('app_consultation_edit', {'id': consultation.id}) }}">edit</a>

    {{ include('consultation/_delete_form.html.twig') }}
   
    
{% endblock %}
