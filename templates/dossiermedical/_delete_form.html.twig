<form method="post" action="{{ path('app_dossiermedical_delete', {'id': dossiermedical.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dossiermedical.id) }}">
    <button class="btn">Delete</button>
</form>
